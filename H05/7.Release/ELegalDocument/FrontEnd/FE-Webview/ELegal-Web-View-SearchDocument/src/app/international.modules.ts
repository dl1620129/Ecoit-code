import {  OwlDateTimeIntl } from 'ng-pick-datetime';

// here is the default text string
export class VietNamIntl extends OwlDateTimeIntl {
    
    /* A label for the up second button (used by screen readers).*/
    upSecondLabel = 'Thêm 1 giây';

    /* A label for the down second button (used by screen readers). */
    downSecondLabel = 'Bớt 1 giây';

    /* A label for the up minute button (used by screen readers). */
    upMinuteLabel = 'Thêm 1 phút';

    /* A label for the down minute button (used by screen readers). */
    downMinuteLabel = 'Bớt 1 phút';

    /* A label for the up hour button (used by screen readers). */
    upHourLabel = 'Thêm 1 tiếng';

    /* A label for the down hour button (used by screen readers).*/
    downHourLabel = 'Bớt 1 tiếng';

    /* A label for the previous month button (used by screen readers).*/
    prevMonthLabel = 'Tháng trước';

    /* A label for the next month button (used by screen readers).*/
    nextMonthLabel = 'Tháng sau';

    /* A label for the previous year button (used by screen readers).*/
    prevYearLabel = 'Năm trước';

    /* A label for the next year button (used by screen readers).*/
    nextYearLabel = 'Năm sau';

    /* A label for the previous multi-year button (used by screen readers).*/
    prevMultiYearLabel = '21 năm trước';

    /* A label for the next multi-year button (used by screen readers).*/
    nextMultiYearLabel = '21 năm sau';

    /* A label for the 'switch to month view' button (used by screen readers).*/
    switchToMonthViewLabel = 'Hiển thị theo tháng';

    /* A label for the 'switch to year view' button (used by screen readers).*/
    switchToMultiYearViewLabel = 'Chọn tháng và năm';

    /* A label for the cancel button */
    cancelBtnLabel = 'Đóng';

    /* A label for the set button */
    setBtnLabel = 'Chọn';

    /* A label for the range 'from' in picker info */
    rangeFromLabel = 'Từ';

    /* A label for the range 'to' in picker info */
    rangeToLabel = 'Đến';

    /* A label for the hour12 button (AM) */
    hour12AMLabel = 'AM';

    /* A label for the hour12 button (PM) */
    hour12PMLabel = 'PM';
};